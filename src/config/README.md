# config layer

Game configuration constants and settings.

Defined here:
- `GAME_WIDTH`, `GAME_HEIGHT`: Canvas dimensions
- `WINNING_SCORE`: Score needed to win
- `PADDLE_HEIGHT`, `PADDLE_WIDTH`: Paddle dimensions
- `BALL_SIZE`: Ball radius
- `INITIAL_BALL_SPEED`: Starting speed
- `SPEED_INCREMENT`: Speed increase per hit
